apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loksamarth-ui.fullname" . }}-test-connection"
  labels:
    {{- include "loksamarth-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loksamarth-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

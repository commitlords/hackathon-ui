apiVersion: v1
kind: Service
metadata:
  name: {{ include "loksamarth-ui.fullname" . }}
  labels:
    {{- include "loksamarth-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loksamarth-ui.selectorLabels" . | nindent 4 }}
